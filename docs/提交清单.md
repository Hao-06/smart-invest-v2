# 初赛提交清单 · 智投未来

> **截止日期**：2026 年 6 月 15 日
> **大赛平台**：https://form.jingyudao.cn/sl/k0pe
> **提交三件套**：📝 作品设计书 · 🎬 演示视频 · 🔗 可运行链接或代码

---

## ✅ 提交前 14 天 — 准备工作

### 1. 验证核心 V2 系统能跑

```bash
# 环境自检
python3 main.py check

# V2 当日推荐（10 秒级，offline 无需 API key）
python3 main.py recommend --date 2026-05-22

# V2 短区间回测（约 1 分钟）
python3 main.py validate --start 2026-05-12 --end 2026-05-22
```

任一命令成功输出 = 系统健康。

### 2. （可选）补一次更近期的真实回测

如果想在视频/设计书里展示最新数据：

```bash
# 跑离当下最近的真实样本外区间
python3 main.py validate --start 2026-05-01 --end 2026-06-10 \
    --name "2026-05~06 临近真样本外" \
    --out runs/v2_recent.json
```

注：offline 模式**无需 LLM**，**零成本**；只用真实行情。

### 3. 设计书数据更新（如果上一步跑了新区间）

`docs/作品设计书.md` 第 7 章已经包含完整的 6 区间 + 连续长回测结果。
**若不补新区间**，直接用现有数据即可，无需改设计书。

---

## ✅ 提交前 7 天 — 必须完成

### 1. 作品设计书 📝

- [ ] [`docs/作品设计书.md`](作品设计书.md) 内容完整（含 7.3 六区间 / 7.4 连续长回测）
- [ ] **填好占位符**：
  - 第 7 行「提交日期：2026 年 ___ 月 ___ 日」
  - （指导教师非必填，单人参赛已删除该行）
- [ ] **导出为 PDF**：

  ```bash
  # Pandoc 示例（推荐）
  pandoc docs/作品设计书.md -o docs/作品设计书.pdf \
    --pdf-engine=xelatex \
    -V mainfont="PingFang SC" \
    -V geometry:margin=2cm
  ```

  也可用 Typora / VS Code Markdown PDF 插件导出。

- [ ] 检查 PDF：无乱码、ASCII 架构图渲染正常、表格对齐
- [ ] **加封面**（PDF 工具或导出前手动加）：

  ```
  ────────────────────────────────────
  首届首都经济贸易大学「驼灵」智能体大赛
  ────────────────────────────────────
            智投未来
    —— LiveTradingAgentV2
       多智能体 A股投研系统
  ────────────────────────────────────
       金融投资赛道  ·  作品设计书
            参赛者：Hao（单人参赛）
            2026 年 6 月
  ────────────────────────────────────
  ```

### 2. 演示视频 🎬

- [ ] 按 [`docs/演示视频脚本.md`](演示视频脚本.md) 录制（已对齐 V2 架构）
- [ ] **时长 4'30" – 5'00"**
- [ ] 1080p / mp4
- [ ] 音频清晰、无杂音、关键数字念对（+18.13 pp / +71.05% / 通过 v9 死亡测试）
- [ ] **不出现 API key、密码、私人信息**
- [ ] 文件命名：`智投未来_LiveTradingAgentV2_演示视频.mp4`

### 3. 可运行链接或代码 🔗

#### 选项 A · GitHub 仓库（**推荐**）

- [ ] 创建 GitHub 仓库（私有先 OK，提交时改公开）
- [ ] **再次确认 `.gitignore` 屏蔽 `.env`**（命令：`git check-ignore .env`，输出 `.env` 即正常）
- [ ] `git status` 确认 `.env` 不在待提交清单里
- [ ] 写一份漂亮的 README（项目已有，可直接复用）
- [ ] commit 历史干净：可选 `git rebase -i` 把 WIP 合并为有意义的几个 commit
- [ ] push 到 GitHub
- [ ] **检查 GitHub 网页上 `.env` 没有出现**
- [ ] 仓库公开 / 给评委加 Read 协作者权限

```bash
# 完整流程参考：
cd "/Users/hao/Downloads/驼灵智能体大赛"
git init                        # 若还没 init
git add .
git status                      # 必看：确认没有 .env
git commit -m "feat: LiveTradingAgentV2 智投未来多智能体A股投研系统 v2.0"

# 在 GitHub 创建空仓库 tuoling-zhitou，然后：
git remote add origin git@github.com:<你的用户名>/tuoling-zhitou.git
git branch -M main
git push -u origin main
```

#### 选项 B · 仅打包 zip（最低保底）

如果不想 push GitHub：

- [ ] 删掉 `.env`、`data_cache/`、`runs/`、`__pycache__/`、`.bak` 备份
- [ ] zip 整个项目目录
- [ ] 文件命名：`智投未来_LiveTradingAgentV2_源码.zip`

#### 选项 C · Streamlit Community Cloud 部署 V2 看板（**强烈推荐**）

部署 `app/dashboard_v2.py` —— V2 专属看板，评委直接点开链接就能看到完整的业绩长卷 + 决策审计 + 当日推荐。

- [ ] GitHub 仓库已就绪
- [ ] 在 https://share.streamlit.io/ 用 GitHub 登录
- [ ] New app → 选你的仓库 → 主文件路径 **`app/dashboard_v2.py`**（V2 专属，**不是**旧的 `dashboard.py`）
- [ ] 提交前确认 `runs/v2_continuous_2024_2026.json` + `runs/v2_region_*.json` 已 commit
  （这是 Tab 1/2 的数据源，部署后秒级加载，无需重算）
- [ ] Advanced settings 配置 **Secrets**（**可选**，仅 Tab 3 勾选「启用 LLM 推理」时才需要）：
  ```toml
  DEEPSEEK_API_KEY = "sk-你的key"
  ```
- [ ] Deploy，测试 3 个 Tab 都能跑通
- [ ] 链接写进设计书 + 提交平台

> 早期的 `app/dashboard.py`（6 分析师架构）仍保留用于个股 K 线/资金流的数据探索，
> 但**评委演示主路径是 `dashboard_v2.py`** —— 它才是与 V2 真正对应的可视化。

---

## ✅ 提交当天

### 在 https://form.jingyudao.cn/sl/k0pe 上传：

- [ ] 作品名称：**智投未来 · LiveTradingAgentV2 多智能体 A股投研系统**
- [ ] 选择赛道：**💹 智投未来（金融投资）**
- [ ] 上传作品设计书 PDF
- [ ] 上传演示视频
- [ ] 填入 GitHub 链接 / Streamlit 部署链接（如有）
- [ ] 填入团队信息（单人参赛；指导老师栏选填，可留空或填"无"）

### 提交后最后核验

- [ ] **点开 GitHub 链接**，确认 README 渲染、目录结构 OK
- [ ] **clone 一份到新机器验证 `python3 main.py recommend --date <某交易日>` 能跑出 JSON**
- [ ] 截图保存提交成功页面
- [ ] 在通讯录里联系老师确认收到

---

## 🚨 安全自检（绝不能犯的错）

- [ ] `.env` 文件**不在 GitHub 上**（在仓库页面搜索 `DEEPSEEK_API_KEY` 应该只能看到 `.env.example` 里的占位符）
- [ ] 演示视频里**没有暴露 API key**（录制前关掉所有可能弹出 key 的窗口）
- [ ] **代码里没有硬编码 key**

```bash
# 一键安全自检命令：
cd "/Users/hao/Downloads/驼灵智能体大赛"
echo "=== 1. .env 是否被 git 忽略 ==="
git check-ignore -v .env || echo "✗ 危险：.env 未被忽略！"
echo "=== 2. 当前 staging 区是否含 .env ==="
git status --porcelain | grep -E "\.env$" && echo "✗ 危险：.env 在待提交清单！" || echo "✓ 安全"
echo "=== 3. 仓库里是否含 sk- 开头的字符串 ==="
git ls-files | xargs grep -l "sk-[a-zA-Z0-9]\{32\}" 2>/dev/null && echo "✗ 危险：发现疑似 key！" || echo "✓ 安全"
```

---

## 📞 联系信息

- **赛事主办**：人工智能学院周老师、闫老师
- **电话**：010-83952376
- **邮箱**：ai@cueb.edu.cn
- **大赛通知**：https://ciyuan-ai.fun/tuoling-competition.html
- **金融投资赛道命题**：https://ciyuan-ai.fun/ai4value-finance-track.html

---

## 后续（复赛阶段）

如果入围前 20 名进决赛：

- [ ] 关注复赛技术对接文档（命题说明里提到「细则以技术对接文档为准」）
- [ ] **按真实平台规则校准** `test_live_agent_v2.py` 中的 `--hold-days` 参数（默认 T+5 是假设）
- [ ] 实盘启用 LLM 推理：`python3 main.py recommend --date <today> --use-llm`，R1 思维链可作为可解释的硬物证
- [ ] 参与「智投未来」平台对接联调
- [ ] 7 月初按评审反馈做作品优化
- [ ] 7 月中旬现场答辩
